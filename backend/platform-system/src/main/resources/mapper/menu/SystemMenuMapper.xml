<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 文件作用：保存全局菜单及权限资源的动态集合校验 SQL。 -->
<mapper namespace="com.xtong.saas.system.menu.mapper.SystemMenuMapper">
    <!-- 统计非空 ID 集合中启用、未删除且允许租户角色分配的菜单和权限资源。 -->
    <select id="countEnabledTenantAssignableByIds" resultType="long">
        SELECT COUNT(*)
        FROM sys_menu
        WHERE deleted = 0
          AND status = 'ENABLED'
          AND permission_scope = 'TENANT'
          AND id IN
          <foreach collection="menuIds" item="menuId" open="(" separator="," close=")">
              #{menuId}
          </foreach>
    </select>

    <!-- 包含逻辑删除记录检查权限码唯一性，更新时排除当前菜单。 -->
    <select id="countByPermissionCodeIncludingDeleted" resultType="long">
        SELECT COUNT(*) FROM sys_menu
        WHERE permission_code = #{permissionCode}
        <if test="excludedMenuId != null">
            AND id != #{excludedMenuId}
        </if>
    </select>

    <!-- 逻辑删除全局菜单并记录真实更新审计。 -->
    <update id="logicalDeleteWithAudit">
        UPDATE sys_menu
        SET deleted = 1, updated_by = #{auditorId}, updated_at = #{updatedAt}
        WHERE id = #{menuId} AND deleted = 0
    </update>
</mapper>
